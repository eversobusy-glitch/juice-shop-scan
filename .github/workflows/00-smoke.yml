# .github/workflows/00-smoke.yml
name: CI smoke & integrity
on:
  push: { branches: [ master, main ] }
  pull_request:
jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4      # Get the repo onto the runner
      - name: Verify Node project
        run: |
          ls -la                        # Evidence in logs
          test -f package.json && echo "Found package.json ✅"
